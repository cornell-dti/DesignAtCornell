import React from "react";
import { FilterCheckboxProps } from "../data-structures/PropertyTypes";

const FilterCheckbox = ({ checked, onClick }: FilterCheckboxProps) => (
    <></>
);

export default FilterCheckbox;