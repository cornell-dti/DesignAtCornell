import React from "react";
import { FilterDropdownProps } from "../data-structures/PropertyTypes";

const FilterDropDown = ({ checkboxData, onChange }: FilterDropdownProps) => (
    <></>
);

export default FilterDropDown;
